// tslint:disable-next-line: interface-over-type-literal
export type Banner = {
    imageUrl: string;
    url: string;
    targetId: number;
};
// 定义数据类型
export type HotTags = {
    name: string;
    id: number;
    position: number;
};
// SongSheet 歌单数据类型
export type SongSheet = {
    name: string;
    picUrl: string;
    playCount: number;
    id: number;
};
